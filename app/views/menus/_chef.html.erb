<%= form_for menu_recipe.first, method: "PATCH", remote: true do |f|%>
  <%= f.hidden_field :menu_id, :value => dinner.menu.id %>
  <%= f.hidden_field :recipe_id, :value => recipe.id %>
  <%= f.submit "Claim this recipe" %>
<% end %>